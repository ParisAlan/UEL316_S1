{% extends 'base.html.twig' %}

{% block title %}{{ article.title }}{% endblock %}

{% block body %}
<div class="container">
    <div class="crud-container">
        <h1>👁️ Détails de l'Article</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>ID</th>
                    <td>{{ article.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ article.title }}</td>
                </tr>
                <tr>
                    <th>Contenu</th>
                    <td>{{ article.content|raw }}</td>
                </tr>
                <tr>
                    <th>Image</th>
                    <td>
                        {% if article.image %}
                            <img src="{{ asset('articles/' ~ article.image) }}" alt="{{ article.title }}" style="max-width: 300px; border-radius: 10px;">
                        {% else %}
                            Aucune image
                        {% endif %}
                    </td>
                </tr>
                <tr>
                    <th>Publié le</th>
                    <td>{{ article.publishedAt ? article.publishedAt|date('d/m/Y à H:i') : 'Non publié' }}</td>
                </tr>
                <tr>
                    <th>Modifié le</th>
                    <td>{{ article.updatedAt ? article.updatedAt|date('d/m/Y à H:i') : 'Jamais modifié' }}</td>
                </tr>
            </tbody>
        </table>

        <div class="crud-actions">
            <a href="{{ path('app_article_index') }}">← Retour à la liste</a>
            <a href="{{ path('app_article_edit', {'id': article.id}) }}">✏️ Modifier</a>
            {{ include('article/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
