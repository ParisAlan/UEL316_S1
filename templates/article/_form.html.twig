{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.title, {'label': 'Titre de l\'article'}) }}
    </div>
    
    <div class="form-group">
        {{ form_row(form.content, {'label': 'Contenu'}) }}
    </div>
    
    <div class="form-group">
        {{ form_row(form.image, {'label': 'Image'}) }}
    </div>
    
    {% if form.publishedAt is defined %}
    <div class="form-group">
        {{ form_row(form.publishedAt, {'label': 'Date de publication'}) }}
    </div>
    {% endif %}
    
    <button type="submit" class="btn">{{ button_label|default('💾 Enregistrer') }}</button>
{{ form_end(form) }}
